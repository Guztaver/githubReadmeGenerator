{
  "dependencies": {
    "prompt-sync": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.24"
  },
  "scripts": {
    "grd::build": "mkdir -p build; tsc",
    "grd::run": "node build/main.js"
  }
}
